---
# Instalar agente CloudWatch
- name: Descargar agente CloudWatch
  ansible.builtin.get_url:
    url: https://s3.amazonaws.com/amazoncloudwatch-agent/amazon_linux/amd64/latest/amazon-cloudwatch-agent.rpm
    dest: /tmp/amazon-cloudwatch-agent.rpm
    mode: '0644'
  tags: cloudwatch

- name: Instalar agente CloudWatch
  ansible.builtin.dnf:
    name: /tmp/amazon-cloudwatch-agent.rpm
    state: present
    disable_gpg_check: yes
  tags: cloudwatch

# Configurar agente CloudWatch
- name: Crear directorio de configuración CloudWatch
  ansible.builtin.file:
    path: /opt/aws/amazon-cloudwatch-agent/etc
    state: directory
    mode: '0755'
  tags: cloudwatch

- name: Crear configuración del agente CloudWatch
  ansible.builtin.copy:
    dest: /opt/aws/amazon-cloudwatch-agent/etc/amazon-cloudwatch-agent.json
    content: |
      {
        "agent": {
          "metrics_collection_interval": 60,
          "logfile": "/opt/aws/amazon-cloudwatch-agent/logs/amazon-cloudwatch-agent.log"
        },
        "metrics": {
          "namespace": "{{ project_name }}/Nodes",
          "metrics_collected": {
            "cpu": {
              "measurement": ["cpu_usage_idle", "cpu_usage_user", "cpu_usage_system"],
              "metrics_collection_interval": 60
            },
            "mem": {
              "measurement": ["mem_used_percent", "mem_available"],
              "metrics_collection_interval": 60
            },
            "disk": {
              "measurement": ["disk_used_percent"],
              "metrics_collection_interval": 60,
              "resources": ["/"]
            }
          }
        },
        "logs": {
          "logs_collected": {
            "files": {
              "collect_list": [
                {
                  "file_path": "/var/log/messages",
                  "log_group_name": "{{ cloudwatch_log_group }}",
                  "log_stream_name": "{instance_id}/messages"
                },
                {
                  "file_path": "/var/log/secure",
                  "log_group_name": "{{ cloudwatch_log_group }}",
                  "log_stream_name": "{instance_id}/secure"
                }
              ]
            }
          }
        }
      }
    mode: '0644'
  tags: cloudwatch

# Iniciar agente CloudWatch
- name: Iniciar agente CloudWatch
  ansible.builtin.service:
    name: amazon-cloudwatch-agent
    state: started
    enabled: yes
  ignore_errors: yes
  tags: cloudwatch
